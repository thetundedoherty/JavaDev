<?xml version="1.0" encoding="utf-8"?>
<resources>
    <!-- Margin around the floating action button in List Item -->
    <dimen name="share_image_margin">3dp</dimen>

    <!-- Margin Top for the profile name textView -->
    <dimen name="text_view_layout">10dp</dimen>

    <!-- Layout dimension for the List item -->
    <dimen name="layout_dimens">75dp</dimen>

    <!-- Layout margin for the imageView-->
    <dimen name="image_layout_margin">12dp</dimen>

    <!-- Text size for the profile name textView -->
    <dimen name="profile_name_text_view_size">18sp</dimen>

    <!-- Text size for the profile url  textView -->
    <dimen name="profile_url_text_view_size">14sp</dimen>

    <!-- The divider height for the ListView  -->
    <dimen name="divider_height">1dp</dimen>

    <!-- Text size for the Id textView -->
    <dimen name="id_text_view_size">12sp</dimen>

    <!-- Text size for the Error TextView -->
    <dimen name="error_text_view_size">16sp</dimen>

    <!-- Padding for the list item -->
    <dimen name="padding">6dp</dimen>

    <!-- layout width and height for the list item -->
    <dimen name="share_image_dimens">18dp</dimen>

    <dimen name="network_image_view_size">185dp</dimen>

    <!-- The profile url textView layout -->
    <dimen name="profile_url_layout">32dp</dimen>

    <!-- The width of both textView in linear layout -->
    <dimen name="linear_layout_child_width">0dp</dimen>

    <!-- The width of both textView in linear layout -->
    <dimen name="placeholder_text_view">30sp</dimen>

    <!-- Linear Layout margin for TextView in activity_detail xml -->
    <dimen name="linear_layout_margin">20dp</dimen>

    <!-- Layout margin for the divider view in activity_detail xml  -->
    <dimen name="view_layout_margin">5dp</dimen>

    <!-- Layout margin left and right for the divider view in activity_detail xml  -->
    <dimen name="view_layout_margin_left_right">16dp</dimen>

    <!-- FloatingActionButton layout margin top -->
    <dimen name="fab_layout_margin_top">75dp</dimen>

</resources>